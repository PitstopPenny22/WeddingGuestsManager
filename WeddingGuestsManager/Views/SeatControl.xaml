<Border
    x:Class="WeddingGuestsManager.Views.SeatControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:viewModels="clr-namespace:WeddingGuestsManager.ViewModels"
    mc:Ignorable="d" 
    Height="40" 
    Width="40"
    d:DataContext="{d:DesignInstance viewModels:SeatViewModel, IsDesignTimeCreatable=false}"
    HorizontalAlignment="Center">
    <TextBlock Text="{Binding SeatNumber}" ToolTip="{Binding AssignedGuest, Mode=OneWay}" HorizontalAlignment="Center" TextAlignment="Center" FontSize="20" VerticalAlignment="Center"/>
</Border>
